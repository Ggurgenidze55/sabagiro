import SwiftUI

enum SabagiroTheme {
  /// Site `--acid` (#f9c108)
  static let acid = Color(red: 249 / 255, green: 193 / 255, blue: 8 / 255)
  static let background = Color(red: 0.04, green: 0.04, blue: 0.04)
}
