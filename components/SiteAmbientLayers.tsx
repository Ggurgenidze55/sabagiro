/** App pages: solid black only. Matrix lives on public/index.html (homepage). */
export function SiteAmbientLayers() {
  return <div className="site-page__ambient site-page__ambient--plain" aria-hidden />;
}
